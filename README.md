# AIFlow Image Production Skill

Complex image-production workflow built on `aiflow-basic`.

Use it for candidate rounds, reference consistency, art direction, deterministic crops, compositing and release QA. For ArcheBase-branded work, also install `archebase-vi-guide`.

Required Python dependency for deterministic image utilities:

```sh
python3 -m pip install Pillow
```
